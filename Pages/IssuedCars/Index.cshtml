@page
@model OrderingCars.Pages_IssuedCars.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IssuedCar[0].Car)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IssuedCar[0].Client)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IssuedCar[0].IssuedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IssuedCar[0].ReturnDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IssuedCar[0].ServiceCost)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.IssuedCar) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Car.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Client.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IssuedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReturnDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ServiceCost)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
